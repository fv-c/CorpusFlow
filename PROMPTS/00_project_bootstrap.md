# Phase 00: Project Bootstrap

- Goal: create the crate, guidance files, docs skeleton, CLI scaffold, and module skeletons.
- Deliverables: buildable workspace, initial config types, source layout, focused tests.
- Constraints: no DSP implementation yet, no heavy dependencies, no speculative abstractions.
