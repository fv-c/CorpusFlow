# Testing Skill

- Add focused unit tests with each new baseline module.
- Prefer deterministic fixtures and exact expected values where practical.
- Test config validation and command parsing before DSP-heavy stages.
- Add microbenchmarks once a repeated processing loop exists.
- Avoid broad integration tests until stage seams are stable.
